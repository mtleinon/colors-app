import React, { Component } from 'react';
//import './SingleColorPalette.css';

export default class SingleColorPalette extends Component {
  // constructor(props) {
  // super(props);

  // this.state = {
  // : '',
  // };
  // }

  // handleChange = e => {
  // this.setState({ [e.target.name]: e.target.value });
  // };

  render() {
    return <h1>SingleColorPalette</h1>;
  }
}
